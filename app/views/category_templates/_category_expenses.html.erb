<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Expenses

    <a data-toggle="collapse" href="#category_expenses_new_form">
      Add expense
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @category.expenses.each do |expense| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/expenses/<%= expense.id %>">
          <%= expense.group_id %>
        </a>

        <a href="/delete_expense_from_category/<%= expense.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="category_expenses_new_form">
        <%= render partial: "category_templates/category_expenses_new_form" %>
      </div>
    </div>

  </ul>
</div>
